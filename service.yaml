apiVersion: v1
kind: Service
metadata:
  labels:
    app: helloworld
  name: helloworld
spec:
  selector:
    app: helloworld
  type: LoadBalancer
  ports:
    - name: "5000"
      port: 5000
      protocol: TCP
      targetPort: 5000

status:
  loadBalancer: {}
